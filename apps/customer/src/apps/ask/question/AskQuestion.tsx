import { AskQuestionContainer } from '@street-vendor/core';
import { pathName } from '../../../configs/pathName';

export const AskQuestion = () => {
  return <AskQuestionContainer pathName={pathName.ASK.INQUIRY.FORM} />;
};
