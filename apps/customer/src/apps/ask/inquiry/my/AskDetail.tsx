import { AskDetailContainer } from '@street-vendor/core';
import { pathName } from '../../../../configs/pathName';

export const AskDetail = () => {
  return <AskDetailContainer pathName={pathName.ASK.INQUIRY.FORM} />;
};
